{
  "name": "copilot-terminal-monitor",
  "displayName": "Copilot Terminal Monitor",
  "description": "Monitors terminal commands and notifies if no output is received for a specified duration.",
  "version": "1.4.2",
  "publisher": "LunarWerx",
  "author": {
    "name": "LunarWerx",
    "url": "https://github.com/LunarWerx"
  },
  "homepage": "https://github.com/LunarWerxs/CopilotTerminalMonitor",
  "repository": {
    "type": "git",
    "url": "https://github.com/LunarWerxs/CopilotTerminalMonitor"
  },
  "bugs": {
    "url": "https://github.com/LunarWerxs/CopilotTerminalMonitor/issues"
  },
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.108.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "terminal-idle-monitor.openSettings",
        "title": "Terminal Monitor: Open Settings"
      },
      {
        "command": "terminal-idle-monitor.showMenu",
        "title": "Terminal Monitor: Show Menu"
      },
      {
        "command": "terminal-idle-monitor.enable",
        "title": "Terminal Monitor: Enable Monitoring"
      },
      {
        "command": "terminal-idle-monitor.disable",
        "title": "Terminal Monitor: Disable Monitoring"
      }
    ],
    "configuration": {
      "title": "Terminal Idle Monitor",
      "properties": {
        "terminalIdleMonitor.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable terminal idle monitoring."
        },
        "terminalIdleMonitor.idleTimeout": {
          "type": "number",
          "default": 60,
          "description": "Time in seconds before notifying about an idle terminal."
        },
        "terminalIdleMonitor.totalTimeout": {
          "type": "number",
          "default": 5,
          "description": "Time in minutes before notifying about total execution time."
        },
        "terminalIdleMonitor.statusBarAlwaysVisible": {
          "type": "boolean",
          "default": true,
          "description": "Keep the status bar item visible even when no command is running."
        },
        "terminalIdleMonitor.displayIdleText": {
          "type": "boolean",
          "default": false,
          "description": "Show 'Idle' text in the status bar when no command is running."
        },
        "terminalIdleMonitor.statusBarAlignment": {
          "type": "string",
          "enum": [
            "Left",
            "Right"
          ],
          "default": "Left",
          "description": "The alignment of the status bar item."
        },
        "terminalIdleMonitor.obnoxiousMode": {
          "type": "boolean",
          "default": false,
          "description": "Enable high-intensity alerts (modal popups and flashing UI)."
        },
        "terminalIdleMonitor.obnoxiousColor": {
          "type": "string",
          "default": "#ff0000",
          "description": "The color to use for flashing the UI in obnoxious mode."
        },
        "terminalIdleMonitor.obnoxiousModeTime": {
          "type": [
            "number",
            "null"
          ],
          "default": null,
          "description": "The time in seconds when obnoxious mode triggers. Defaults to blank which uses the idle timeout."
        },
        "terminalIdleMonitor.obnoxiousSnooze": {
          "type": "boolean",
          "default": true,
          "description": "If enabled, snoozing a notification will make the next alert obnoxious."
        },
        "terminalIdleMonitor.obnoxiousPerWindow": {
          "type": "boolean",
          "default": true,
          "description": "If enabled, obnoxious flashing will only affect the current window (requires a workspace to be open)."
        },
        "terminalIdleMonitor.enableExclusions": {
          "type": "boolean",
          "default": false,
          "description": "Enable terminal title exclusions."
        },
        "terminalIdleMonitor.excludePatterns": {
          "type": "string",
          "default": "",
          "description": "Comma-separated list of terminal titles to exclude from monitoring (e.g., 'npm: watch*', 'debug')."
        },
        "terminalIdleMonitor.onlyMonitorActive": {
          "type": "boolean",
          "default": true,
          "description": "Only monitor the terminal currently visible. Background terminals will be ignored."
        },
        "terminalIdleMonitor.useSigInt": {
          "type": "boolean",
          "default": true,
          "description": "When terminating, send Ctrl+C (SIGINT) instead of closing the terminal window."
        },
        "terminalIdleMonitor.autoTerminateEnabled": {
          "type": "boolean",
          "default": false,
          "description": "Automatically terminate idle terminals after a specific duration."
        },
        "terminalIdleMonitor.autoTerminateTimeout": {
          "type": "number",
          "default": 10,
          "description": "Time in minutes before an idle terminal is automatically terminated."
        },
        "terminalIdleMonitor.hardTerminateRetries": {
          "type": "number",
          "default": 3,
          "description": "Number of Ctrl+C retries before a hard termination (closing the terminal) occurs. Only used if Gentle Termination is enabled."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.108.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.52.0",
    "eslint": "^9.39.2",
    "esbuild": "^0.27.2",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
